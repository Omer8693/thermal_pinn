__pycache__/
*.pyc
*.pyo
.DS_Store
.vscode/

# Model checkpoints (large binary — tracked via metrics JSON only)
checkpoints/**/*.pt
checkpoints/**/*.pt.bak

# Thesis report (LaTeX source, not part of the code repo)
reports/
